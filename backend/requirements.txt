# Core API framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Added now, used starting Day 2 (database + Redis setup).
# Installing them today avoids a second round of "pip install" and
# dependency-conflict debugging in the middle of tomorrow's work.
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2
redis==5.0.8
# PostGIS geometry support for SQLAlchemy - needed to map the
# boundary_geofence (institutions) and affected_polygon (emergency_alerts)
# columns into Python types. Base PostGIS extension is created inside the
# database; this package is only the Python-side type binding.
GeoAlchemy2==0.15.2
# Async HTTP client used by the NDMA SACHET CAP feed poller
# (app/services/cap_ingestion.py). Kept here so a fresh environment
# install is enough to run the full backend.
httpx==0.27.2

# Testing dependencies
pytest==8.3.3
pytest-asyncio==0.23.8
websockets==12.0

# JWT for WebSocket authentication
pyjwt==2.9.0
